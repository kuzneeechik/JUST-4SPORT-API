server:
  port: 8081

spring:
  application:
    name: just_4sport
  datasource:
    url: jdbc:postgresql://localhost:5432/JUST4SPORT
    username: ***
    password: ***
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml
